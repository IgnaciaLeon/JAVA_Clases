package C730082023;

public class Encapsulacion_y_abstraccion {

 //Encapsulacion: El principio de encapsulamiento permite modificar el acceso a la clase, sus atributos y métodos. Es
    // decir, que va a restringir o no la información respectiva a esa clase, dependiendo de donde me sitúe en el programa.
    //La idea de la encapsulación es proteger los datos de la modificación fácl, con los modificadores de acceso publico
    // o privado.
    // PUBLIC: cuando indicamos en los modificadores de acceso como public estamos usando encapsulacion. cuando está
    // public indico que desde cualquier otro lado de la app puedo acceder a su informacion.

    //PROTECTED: el modificador de acceso protected me deja acceder a un dato siempre y cuando me encuentre en el mismo
    // package, es decir, que me permite acceder a lo que hay dentro de package y de sus clases hijas (incluso si están
    // en otro package).
    //DEFAULT: permite acceder a lo que hay dentro del mismo package.
    //PRIVATE: no me deja acceder a los datos a menos de que acceda a ellos a través de un setter.

  //Abstracción:
    //se utiliza para definir una categoría muy macro que no se puede instanciar (?)
  //ej. la abstracción de calculadora podría ser "Maquina" con la funcion "encender y apagar".




}
